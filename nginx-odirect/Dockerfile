FROM nginx

ADD static/med.txt /data/
ADD static/small.txt /data/
ADD static/big.txt /data/
ADD nginx.conf /etc/nginx/nginx.conf
ADD localhost.crt /etc/localhost.crt
ADD localhost.key /etc/localhost.key
